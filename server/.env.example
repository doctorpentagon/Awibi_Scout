# Awibi Scout server
PORT=5188
NODE_ENV=development
# Comma-separated list of allowed browser origins. Leave blank to allow all in development.
CORS_ORIGIN=http://localhost:5173
# Directory holding the release datasets. Defaults to <server>/data
DATA_DIR=
